<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pinkpig.payment.infrastructure.dao.TradeGoodsMapper">

    <resultMap id="GoodResultMap" type="com.pinkpig.payment.domain.trade.model.entity.TradeGoodsEntity">
        <id column="id" property="id"/>
        <result column="goods_id" property="goodsId"/>
        <result column="goods_name" property="goodsName"/>
        <result column="price" property="price"/>
        <result column="stock" property="stock"/>
    </resultMap>

    <select id="queryGoods" resultMap="GoodResultMap">
        SELECT * FROM trade_goods WHERE goods_id = #{goodsId}
    </select>

    <update id="deductStock">
        UPDATE trade_goods
        SET stock = stock - 1
        WHERE goods_id = #{goodsId}
        AND stock > 0
    </update>

</mapper>